#!/usr/bin/python3
def add_tuple(tuple_a=(), tuple_b=()):
    if len(my_list) == 0:
        return None
    max = my_list[0]
    for i in my_list:
        if i > max:
            max = i
    return max
